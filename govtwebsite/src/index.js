import React from 'react';
// File: src/index.js
import ReactDOM from 'react-dom';
import App from './App';  // Verify the correct path

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
